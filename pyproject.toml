[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "self-learning-agent"
version = "0.1.0"
description = "An experimental AI agent that learns from experience."
requires-python = ">=3.11"